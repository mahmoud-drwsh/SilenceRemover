# Domain docs

This is a single-context repository.

Before work that touches the media domain, read root `CONTEXT.md`. Read relevant records in `docs/adr/` when that directory exists. Use the glossary's canonical terms in issues, tests, and implementation notes.

When a term is genuinely resolved, update `CONTEXT.md`; reserve ADRs for hard-to-reverse trade-offs.
