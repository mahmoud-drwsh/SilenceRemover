# Required: keep this in .env (do not commit .env)
# Get your API key from https://openrouter.ai
OPENROUTER_API_KEY=your_api_key_here

# All other settings (models, silence params, timeouts, etc.) are configured via
# CLI flags and constants in src/config.py. Environment variables are not used
# for non-secret tuning.
