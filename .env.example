# OpenRouter API Configuration
# Required: Get your API key from https://openrouter.ai
OPENROUTER_API_KEY=your_api_key_here

# OpenRouter Model Configuration
# Transcription model (must support audio input)
# Default: google/gemini-2.0-flash-lite-001
OPENROUTER_DEFAULT_MODEL=google/gemini-2.0-flash-lite-001

# Title generation model (text-only, high quality)
# Default: google/gemini-2.5-flash-lite
# Pricing: $0.10/1M input tokens, $0.40/1M output tokens
OPENROUTER_TITLE_MODEL=google/gemini-2.5-flash-lite

# OpenRouter API Endpoint (optional, rarely needs changing)
OPENROUTER_API_URL=https://openrouter.ai/api/v1/chat/completions

# Silence Detection Parameters
# Noise threshold in dB for silence detection (lower = more sensitive)
# Default: -30.0
NOISE_THRESHOLD=-30.0

# Minimum duration of silence to be detected (seconds)
# Default: 0.5
MIN_DURATION=0.5

# Padding retained around detected silences (seconds)
# Default: 0.5
PAD=0.5

# File System Utilities (Optional - Advanced)
# Timeout for file operations (seconds)
# Default: 30
SILENCE_REMOVER_WAIT_TIMEOUT_SEC=30

# Sleep interval between file operation retries (seconds)
# Default: 0.25
SILENCE_REMOVER_WAIT_SLEEP_SEC=0.25
