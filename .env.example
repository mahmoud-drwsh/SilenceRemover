# Required: keep this in .env (do not commit .env)
# Get your API key from https://openrouter.ai
OPENROUTER_API_KEY=your_api_key_here

# All other settings (models, silence params, timeouts, etc.) have defaults in src/config.py
# and can be overridden via environment variables if needed.
